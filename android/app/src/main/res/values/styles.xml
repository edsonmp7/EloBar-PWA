<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.EloBar" parent="android:style/Theme.Material.NoActionBar">
        <item name="android:windowActionModeOverlay">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/elo_background</item>
        <item name="android:statusBarColor">@color/elo_background</item>
        <item name="android:navigationBarColor">@color/elo_background</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:colorAccent">@color/elo_gold</item>
        <item name="android:fontFamily">sans</item>
    </style>

    <style name="Theme.EloBar.Boot" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/elo_splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/boot_transparent</item>
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="windowSplashScreenAnimationDuration">0</item>
        <item name="postSplashScreenTheme">@style/Theme.EloBar</item>
    </style>
</resources>
